pipeline {

    agent any

    stages {

        stage("build"){
            
            steps{
                echo 'builing the app'
            }
        }

        stage("test"){

            steps{
                echo 'test the app'
            }
        }

        stage("deploy"){

            steps{
                echo 'deploy the app'
            }
        }
    }
}